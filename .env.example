# DevClaw — Environment Variables (OPTIONAL)
#
# The encrypted vault is the recommended way to store secrets:
#   copilot config vault-init
#   copilot config vault-set
#
# This .env file is a fallback for environments where the vault
# is impractical (CI/CD pipelines, Docker, automated deployments).
#
# Resolution order:
#   1. Encrypted vault (.devclaw.vault)
#   2. OS keyring (GNOME/macOS/Windows)
#   3. Environment variables / .env file  ← this file
#   4. config.yaml (plaintext, least secure)
#
# Usage:  cp .env.example .env

# LLM API Key
DEVCLAW_API_KEY=

# Fallback provider keys (checked if DEVCLAW_API_KEY is not set)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Brave Search (for web-search skill)
# BRAVE_API_KEY=

# Gateway auth token (for HTTP API)
# DEVCLAW_GATEWAY_TOKEN=
